<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme (Compose-only). The actual theming is in Theme.kt -->
    <style name="Theme.DarkPlayMC" parent="Theme.AppCompat.NoActionBar">
        <item name="android:statusBarColor">@color/dark_background</item>
        <item name="android:navigationBarColor">@color/dark_background</item>
    </style>

    <!-- Splash screen theme (used in AndroidManifest for the activity) -->
    <style name="Theme.DarkPlayMC.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/dark_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.DarkPlayMC</item>
    </style>
</resources>
